/* FOOTER
-------------------------------------------------*/
.main-footer {
    @extend %space;
    color: $green;
    background: lighten($colorPrimary, 2%);
    text-align: center;
    small {
        display: block;
    }
}
